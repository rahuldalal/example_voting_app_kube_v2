apiVersion: v1
kind: Service
metadata:
  name: voting-app-service
  labels:
    name: voting-app-service
    app: example-voting-app
spec:
  type: LoadBalancer
  ports:
    - port: 5000
      targetPort: 80
  selector:
    name: voting-app-pod
    app: example-voting-app